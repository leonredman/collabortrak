spring.application.name=collabortrak

# Working Deployed Railway App MYSQL with env vars
spring.datasource.url=jdbc:${MYSQL_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Allows my Vercel URL for prod
spring.web.cors.allowed-origin-patterns=https://collabortrak.vercel.app

# Security Settings
spring.security.enabled=true

# Server Port - Railway automatically assigns a port, so read it from environment variables
server.port=${PORT}

spring.jpa.show-sql=true

# Enable debug logging (optional)
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.springframework=INFO
logging.level.com.collabortrak=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE




